if ($(this).hasClass('expand-link')) {
      $(cell.node()).removeClass('truncated').html(fullText + ' <span class="collapse-link">See Less</span>');
    } else {
      $(cell.node()).addClass('truncated').html(fullText.substring(0, limit) + '... <span class="expand-link">See More</span>');
    }
  });
